import {StyleSheet} from 'react-native';

const Styles = StyleSheet.create({
  container: {
    width: '100%',
    height: 50,
    flexDirection: 'row',
  },
  tabBarButton: {
    flex: 1,
    alignItems: 'center',
  },
});

export default Styles;
